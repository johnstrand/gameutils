1. Replace `Jagged_KnownValues` with `Jagged_ValidInput_ReturnsExpectedValues` in `tests/GameUtils.Tests/Animation/OffsetTests.cs` using `replace_with_git_merge_diff`. The new test will use `DataRow` to test values `0.1f`, `0.25f`, `0.5f`, `0.75f`, and `0.9f`, and assert expected values using `Assert.AreEqual` with `Epsilon`.
2. Verify the changes by running `run_in_bash_session` with `grep` to ensure the new method `Jagged_ValidInput_ReturnsExpectedValues` is present in `OffsetTests.cs`.
3. Run `run_in_bash_session` with `dotnet test tests/GameUtils.Tests --filter Jagged` to ensure the new test passes.
4. Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
5. Submit the changes using the `submit` tool.
